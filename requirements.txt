# Whisper pipeline dependencies for the upload server.
#
# Cross-platform on purpose. The CUDA wheels at the bottom carry
# PEP 508 markers so pip skips them on macOS / Windows / Linux-arm64,
# where they don't exist. On Linux x86_64 they install (and are
# pulled in by torch anyway).
#
# Install on a fresh box:
#   python3 -m venv sk
#   sk/bin/pip install --upgrade pip
#   sk/bin/pip install -r requirements.txt
#
# Verify:
#   sk/bin/python -c "import whisper; print('OK', whisper.__file__)"

openai-whisper==20240930
torch==2.5.1
torchaudio==2.5.1
torchvision==0.20.1

# Whisper's transitive deps. Pinned for reproducibility; cross-platform.
certifi==2024.12.14
charset-normalizer==3.4.0
ffmpeg==1.4
filelock==3.16.1
fsspec==2024.12.0
idna==3.10
Jinja2==3.1.5
llvmlite==0.43.0
MarkupSafe==3.0.2
more-itertools==10.5.0
mpmath==1.3.0
networkx==3.4.2
numba==0.60.0
numpy==2.0.2
pillow==11.0.0
regex==2024.11.6
requests==2.32.3
sympy==1.13.1
tiktoken==0.8.0
tqdm==4.67.1
typing_extensions==4.12.2
urllib3==2.3.0

# CUDA wheels — only resolvable on Linux x86_64 with CUDA 12.4. Marked so
# pip skips them on every other platform instead of erroring out the whole
# install. On Linux x86_64 torch pulls them in automatically, so these
# markers also avoid duplicate-install warnings.
nvidia-cublas-cu12==12.4.5.8       ; sys_platform == "linux" and platform_machine == "x86_64"
nvidia-cuda-cupti-cu12==12.4.127   ; sys_platform == "linux" and platform_machine == "x86_64"
nvidia-cuda-nvrtc-cu12==12.4.127   ; sys_platform == "linux" and platform_machine == "x86_64"
nvidia-cuda-runtime-cu12==12.4.127 ; sys_platform == "linux" and platform_machine == "x86_64"
nvidia-cudnn-cu12==9.1.0.70        ; sys_platform == "linux" and platform_machine == "x86_64"
nvidia-cufft-cu12==11.2.1.3        ; sys_platform == "linux" and platform_machine == "x86_64"
nvidia-curand-cu12==10.3.5.147     ; sys_platform == "linux" and platform_machine == "x86_64"
nvidia-cusolver-cu12==11.6.1.9     ; sys_platform == "linux" and platform_machine == "x86_64"
nvidia-cusparse-cu12==12.3.1.170   ; sys_platform == "linux" and platform_machine == "x86_64"
nvidia-nccl-cu12==2.21.5           ; sys_platform == "linux" and platform_machine == "x86_64"
nvidia-nvjitlink-cu12==12.4.127    ; sys_platform == "linux" and platform_machine == "x86_64"
nvidia-nvtx-cu12==12.4.127         ; sys_platform == "linux" and platform_machine == "x86_64"
triton==3.1.0                      ; sys_platform == "linux" and platform_machine == "x86_64"
